import { MyshadowDirective } from './myshadow.directive';

describe('MyshadowDirective', () => {
  it('should create an instance', () => {
    const directive = new MyshadowDirective();
    expect(directive).toBeTruthy();
  });
});
